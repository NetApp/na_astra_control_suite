- name: Create ACC Operator namespace
  kubernetes.core.k8s:
    name: netapp-acc-operator
    api_version: v1
    kind: Namespace
    state: present
    kubeconfig: "{{ hosting_ocp_kubeconfig_path }}"
